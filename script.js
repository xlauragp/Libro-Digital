$(document).ready(function () {
    // Inicializa el booklet para crear el efecto de libro
    $('#mybook').booklet({
        width: 800,
        height: 500,
        autoCenter: true,
        pageNumbers: true,
        arrows: false
    });
});
